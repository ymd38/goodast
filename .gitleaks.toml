title = "Gitleaks configuration for goodast"

# gitleaks のデフォルトルールを継承する。
# 誤検知が出た場合のみ、対象を最小単位（regex / stopword）で allowlist する。
# ファイル全体の allowlist は secret-scan のバイパスになるため行わない。
[extend]
useDefault = true
